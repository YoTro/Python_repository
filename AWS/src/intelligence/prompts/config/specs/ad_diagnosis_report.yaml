id: ad_diagnosis_report
version: "1.0.0"
scope: batch
token_budget: 180000
role_id: senior_ads_analyst
framework_ids: []

required_vars:
  - count
  - report_date
  - _summary_json
  - items_json

optional_vars: {}

output_schema: null

template: |
  You are a senior Amazon advertising analyst. Analyse the following enriched advertising dataset for {count} ASIN(s) and produce a structured diagnostic report strictly following the output format below.

  Report date: {report_date}  (use this to interpret relative dates — e.g., a change_attribution event on 2026-04-17 is 10 days ago; the last entry in natural_rank_series is today's rank.)

  Pre-computed highlights (authoritative — do not re-derive these values):
  {_summary_json}

  Full enriched data (use for keyword-level, attribution, rank-series, trend, and competitor analysis):
  {items_json}

  ==== OUTPUT FORMAT (repeat for each ASIN) ====

  ## ASIN: {{asin}} – Severity: 🟢 Healthy / 🟡 Warning / 🔴 Critical

  ### Quick Metrics Snapshot

  The block below is the authoritative pre-computed summary. Render scalar fields as a three-column table (Field | Value | Source / How derived). Use `null` as-is — do NOT substitute guesses. Do NOT re-derive any value from the full JSON.

  SKIP these fields from the table (they are dicts or lists-of-dicts — render them only in their dedicated report sections, NOT here): lp_summary, lp_top_allocations, campaign_actions, keyword_actions, auto_mining_summary, auto_mining_beta_prior, auto_mining_negatives, auto_mining_harvest.

  Column definitions:
  - **Field**: exact key name from the JSON below.
  - **Value**: exact value (keep null as `—`). For simple arrays (e.g. lp_zero_keywords, rank_tracked_keywords), show the array inline. Never write 'object' — if a value cannot be shown as a scalar or simple array, skip the row.
  - **Source / How derived**: one short phrase from the legend below. ⚠️ Do NOT write 'Pre-computed highlights', 'summary', or any generic phrase — every row must have a *specific* source (e.g. 'SP-API Catalog', 'FBA Inventory API', 'Ads API spCampaigns report', etc.).

  Legend (Field → derivation):
    title / brand / size / bullet_point_count → SP-API Catalog
    lookback_days → days config (default 30); period covered: data_start_date → data_end_date
    data_start_date / data_end_date → reporting window used for all time-series fields (today − lookback_days → yesterday; use these when describing 'past N days')
    total_available → fulfillable FBA units (point-in-time)
    inbound_receiving / inbound_shipped / inbound_working → FBA inbound tiers: receiving=at FC being checked in (1-2d, certain); shipped=in transit from seller (10-30d depending on sea/domestic); working=shipment plan created, not yet shipped (uncertain ETA — do NOT count as arriving soon)
    total_inbound = inbound_receiving + inbound_shipped (confirmed in-transit only)
    effective_stock_days (in campaign_actions/keyword_actions prerequisite) = (total_available + inbound_receiving + catchable_shipped) / daily_sales; catchable_shipped = inbound_shipped only when inbound_lead_days < can_sell_days
    prerequisite field on actions → inventory gate triggered: action downgraded to P2 because effective_stock_days < stock_gate_days (default 21). When reporting these actions, state the prerequisite condition clearly and recommend the seller confirm inbound ETA before activating.
    daily_sales / daily_sales_source → daily_sales = total units sold ÷ lookback_days (ALL channels: ad-attributed + organic). Source priority: (1) caller-supplied, (2) order_metrics = SP-API getOrderMetrics (ad + organic, most accurate), (3) ad_orders_only = spCampaigns ad-attributed orders ÷ days only — organic orders excluded, so daily_sales is a LOWER BOUND on true consumption velocity. When reporting inventory runway, always state the source. When daily_sales_source=ad_orders_only, warn that true stockout will occur SOONER than can_sell_days indicates (organic sales consume inventory too).
    can_sell_days → total_available ÷ daily_sales (null if daily_sales unavailable; when daily_sales_source=ad_orders_only: this is an UPPER BOUND — actual stockout is sooner because organic orders are excluded from the denominator; can_sell_days_note field contains a human-readable caveat)
    inventory_risk → can_sell_days < inventory_risk_days config (default 30 d)
    campaign_count → count of campaigns matched to this ASIN (matching method in campaign_match_strategy)
    campaign_match_strategy → how campaigns were matched to this ASIN: explicit_config (most reliable) | spAdvertisedProduct (ground truth — campaigns that actually delivered ads for this ASIN per report data) | name_substring (fragile — ASIN in campaign name; may include unrelated campaigns) | none
    active_campaign_count → campaigns with state=ENABLED
    paused_campaign_count → campaigns with state=PAUSED
    total_daily_budget → sum of ENABLED campaign daily budgets (Ads API, current config)
    bidding_strategies → distinct strategies across matched campaigns
    total_spend / total_sales / total_orders / total_clicks → spCampaigns ad-attributed performance (ad orders only, organic excluded), summed over matched campaigns, period: data_start_date → data_end_date. NOTE: total_orders here is AD-ATTRIBUTED orders only. True total unit sales (ad + organic) = daily_sales × lookback_days when daily_sales_source=order_metrics.
    account_acos → total_spend ÷ total_sales × 100 (data_start_date → data_end_date)
    budget_exhaustion_pct → (total_spend ÷ (total_daily_budget × lookback_days)) × 100 (data_start_date → data_end_date); percentage scale, e.g. 95.0 = 95%
    budget_likely_exhausted → budget_exhaustion_pct > 90 threshold
    keyword_count / avg_bid / min_bid / max_bid / match_type_dist → Ads API keyword list for matched campaigns (current config, not time-series)
    kw_performance_count → rows in spSearchTerm report with ≥ min_clicks_for_cvr clicks (data_start_date → data_end_date)
    lp_summary / lp_top_allocations / lp_zero_keywords / lp_maxed_keywords → OR-Tools LP (maximise ad orders; constraints: C1 global budget, C2 per-campaign budget caps, C3 target ACOS linearised, C4 click floor/ceiling; inventory is NOT a LP constraint — replenishment timing is too unstable to model as a hard bound; eff_cpc = avg_cpc × bidding_strategy_multiplier × placement_multiplier; pessimistic CVR shrinkage (Beta-Binomial) applied for low-sample keywords). lp_orders_bbs_estimate uses Beta-Binomial shrunk CVR (may be < actual_daily_ad_orders — expected). lp_orders_cvr_matched uses un-shrunk AND un-deflated raw CVR — same basis as actual_daily_ad_orders (both keyword-scope, un-deflated). order_gap = lp_orders_cvr_matched − actual_daily_ad_orders is therefore a fair apples-to-apples comparison: positive means LP reallocation gains orders, negative means LP cannot outperform current allocation within budget. actual_daily_ad_orders = spCampaigns ad-attributed orders ÷ days (AD ORDERS ONLY — organic orders not included; true daily total consumption is higher). order_gap = lp_orders_cvr_matched − actual_daily_ad_orders (positive = LP reallocates budget to gain more ad orders; negative — two distinct causes: (1) spend_ceiling_bound=true: click ceilings prevent the LP from using the full budget — order_gap is ceiling-limited, primary action is expand keywords / raise bids; (2) budget_binding=true (lp_optimal_spend ≥ 85% of daily_budget): budget is exhausted and Wilson CVR shrinkage causes LP's estimated orders to fall below actual historical — this is an artifact of pessimistic estimation, NOT evidence the current allocation beats LP; campaign_actions already corrects for this by allowing increase_budget when ACOS is healthy). CRITICAL — lp_zero_keywords does NOT mean 'inefficient'. A keyword is in lp_zero_keywords because the LP allocated 0 clicks to it. There are TWO distinct reasons — you MUST check keyword_acos_pct before labelling:
    (i) Genuinely low ROI: keyword_acos_pct > target_acos_applied → LP correctly excluded it. Call it 'inefficient' or 'above-target ACOS'; recommend pausing or cutting bid.
    (ii) Budget-crowded-out: keyword_acos_pct ≤ target_acos_applied AND budget_binding=true → the keyword is EFFICIENT but was excluded because the budget was fully consumed by even-higher-ROI keywords first. Do NOT call it 'inefficient'. Call it 'efficient but crowded out by budget'; recommend increasing total budget.
    FORBIDDEN: never label a keyword 'inefficient' or 'low ROI' if its keyword_acos_pct ≤ target_acos_applied. FORBIDDEN: when the same keyword text appears in multiple campaigns (different campaign_ids), treat each campaign instance separately — ACOS and LP allocation may differ per campaign. Always qualify keywords with their campaign_id when making efficiency or action statements.
    CRITICAL — TWO DISTINCT LP NARRATIVES (never conflate them):
    NARRATIVE A — Reallocation opportunity (order_gap > 0): LP found a better way to distribute the SAME total budget. The gap is caused by ALLOCATION INEFFICIENCY, not budget shortage. Correct framing: 'LP suggests reallocating the existing $X budget could yield +Y orders/day — move spend away from above-target-ACOS keywords toward lp_maxed_keywords (high ROI).' FORBIDDEN when order_gap > 0: do NOT write 'budget constraint is causing the order gap' or 'orders left on table due to budget limit' — the total budget is the SAME; the cause is suboptimal allocation, not insufficient total spend.
    NARRATIVE B — Genuine budget constraint (hold_keyword + budget_binding=true): an efficient keyword (ACOS below target) is starved because the total budget is fully consumed by other keywords. Correct framing: 'Keyword X (campaign Y) is efficient (ACOS $val% < target) but cannot scale — total budget is exhausted; increasing total budget unlocks this keyword.' FORBIDDEN: do NOT conflate NARRATIVE B (need more total budget) with NARRATIVE A (need better allocation). They may both be present, but must be stated separately with distinct root causes.
    When BOTH narratives appear simultaneously, write them as two separate points: '(1) Reallocating the current $X budget per LP plan could gain +Y orders/day (allocation fix — no extra budget needed). (2) Additionally, keyword Z (campaign W) is efficient but budget-constrained — increasing total budget further would unlock Z.'
    CRITICAL — LP budget split (read carefully before writing the Budget section):
    lp_scope_campaign_daily_budget = sum of LP-scope campaign daily budgets — the actual global constraint given to LP. LP cannot spend more than this.
    total_account_daily_budget = sum of ALL enabled campaign daily budgets (LP-scope + non-LP-scope).
    lp_scope_daily_spend = historical daily spend for LP-scope campaigns (what they actually spent).
    non_lp_scope_daily_spend = historical daily spend for non-LP campaigns (auto/PT + untracked keywords).
    lp_optimal_spend = what LP recommends spending across LP-scope keywords.
    FORBIDDEN: do NOT add lp_optimal_spend + non_lp_scope_daily_spend and compare to total_account_daily_budget — these come from SEPARATE campaign budget pools and adding them produces a meaningless number that is always larger than the account budget. The correct comparisons are:
    lp_optimal_spend vs lp_scope_campaign_daily_budget (is LP using its allocated budget?)
    lp_scope_daily_spend vs lp_scope_campaign_daily_budget (did LP-scope campaigns historically hit cap?)
    NOTE: lp_scope_daily_spend > lp_scope_campaign_daily_budget by ≤25% is normal — Amazon's dynamic bidding allows intra-day overspend and the Ads console averages across the month; do NOT flag this as a contradiction.
    non_lp_scope_daily_spend vs (total_account_daily_budget − lp_scope_campaign_daily_budget) (did non-LP campaigns hit their budget cap?)
    budget_source: 'campaign_snapshot' = current campaign budget snapshot; 'historical_avg_spend' = historical_daily_spend × 0.90 used instead because discrepancy exceeded Amazon's 25% daily pacing allowance (structural budget change detected). budget_overage_ratio = historical_daily_spend ÷ campaign_snapshot_budget: ratio ≤ 1.25 is normal Amazon pacing (campaigns regularly hit cap → Amazon allows up to 25% daily overage); ratio > 1.25 indicates campaigns ran at higher budgets during the period (budget was cut recently). When budget_overage_ratio is 1.20–1.25 and budget_source='campaign_snapshot': actual achievable spend ≈ total_account_daily_budget × budget_overage_ratio — mention this in the report as 'Amazon budget pacing: account consistently hits cap, actual spend runs ~X% above set budget'; this is NOT a data error. Do NOT flag budget_overage_ratio ≤ 1.25 as a discrepancy or data quality issue. spend_ceiling_bound=true means click ceilings (C4) are the binding constraint, NOT the budget (lp_optimal_spend < 60% of daily_budget); in this case: (a) lp_zero/maxed keyword signals remain valid as ROI signals; (b) primary recommendation is expand keyword coverage or raise bids (not increase budget). budget_binding=true means the global budget cap is fully consumed; a negative order_gap when budget_binding=true is expected and should NOT be flagged as a caveat. placement_data_unknown=true means all spend was reported under 'UNKNOWN' placement — do NOT make placement-specific recommendations in this case. recommended_stock_units = (organic_daily + lp_orders_cvr_matched) × stock_gate_days — total units required to safely execute the LP plan for stock_gate_days (default 21d) without stockout risk; daily_consumption = organic daily orders + lp_orders_cvr_matched (LP-projected total consumption rate, NOT the same as the rate used for can_sell_days); can_sell_days uses actual historical daily_sales (ad+organic) which is higher than lp_raw_orders alone — so daily_consumption < actual consumption rate is EXPECTED and NOT a contradiction; daily_consumption_source: 'order_metrics' = ad+organic (accurate), 'ad_orders_only' = underestimate (organic excluded — real shortfall is larger); inbound_receiving / inbound_shipped / inbound_working / total_inbound are top-level snapshot fields (not inside lp_summary); confirmed_inbound = inbound_receiving + inbound_shipped (units already in transit, offset against shortfall); inbound_working = shipment plan not yet shipped — NOT counted in confirmed_inbound (arrival timing unknown); stock_shortfall = max(0, recommended_stock_units − total_available − confirmed_inbound) — net units still to procure after accounting for current stock and confirmed inbound; when stock_shortfall > 0: lead with inventory procurement recommendation before bid/budget actions; when reporting: state the full breakdown — current stock X units + inbound_receiving Y units (1-2d) + inbound_shipped Z units (ETA varies) = coverage; if inbound_working > 0, note it separately as 'planned but not shipped — confirm dispatch urgently'; CRITICAL — stock_shortfall=0 AND effective_stock_days < stock_gate_days can BOTH be true simultaneously and is NOT a contradiction: stock_shortfall uses total confirmed pipeline (total_available + inbound_receiving + inbound_shipped) which may cover the 21-day target in aggregate; effective_stock_days uses only near-term confirmed stock (total_available + inbound_receiving, 1-2d ETA) which may fall short of stock_gate_days; when this coexists, write: 'Total pipeline sufficient (stock_shortfall=0), but near-term stock covers only {{effective_stock_days}}d — the inventory gate will clear when inbound_shipped units arrive (ETA 10-30d).'
    campaign_actions → two evaluation paths based on LP scope:
      (a) LP-scoped campaigns (manual keyword targeting with click data): action derived from LP camp_spend vs actual campaign budget; rationale will NOT contain 'outside LP scope'.
      (b) Non-LP-scope campaigns (auto-targeting, product-targeting, or manual with zero click data): action derived from ACOS only — high ACOS → decrease_budget/review_bids, healthy ACOS + saturated → increase_budget, otherwise maintain; rationale will contain '(auto/PT, outside LP scope)'; for high-ACOS auto campaigns recommend adding NEGATIVE keywords/targets, NOT lowering keyword bids; for high-ACOS PT campaigns recommend removing or negating underperforming targets.
    campaign_state field present; action ∈ {{increase_budget, decrease_budget, review_bids, pause_candidate, archive_candidate, enable_and_increase_budget, enable_and_review_bids, maintain}}; priority ∈ {{P0, P1, P2}} — defined as follows and applied uniformly across all action types:
      P0 = Immediate / revenue-blocking: situation is actively losing money or at imminent risk (budget exhausted daily, ACOS > 130% of target, stockout within stock_gate_days). Surface in Top 5; do NOT demote.
      P1 = High-priority / act this week: measurable problem exists but not yet critical (ACOS above target, keyword genuinely inefficient, LP-causal conflict, paused campaign with healthy ACOS). Include in Top 5 unless a conflict rule overrides.
      P2 = Monitor / conditional: no urgent action; revisit when a condition changes (hold_keyword waiting for budget, increase_bid gated by inventory, maintain, insufficient data). Mention as secondary notes; omit from Top 5 if P0/P1 items fill the slots.
    actions prefixed enable_and_* mean campaign is PAUSED — re-enable BEFORE any budget/bid change
    keyword_actions → LP-derived keyword-level recommendations; action ∈ {{pause_keyword, hold_keyword, increase_bid, decrease_bid}}; fields: keyword_text, match_type, campaign_id, keyword_id (Ads API id for direct API call), current_bid, keyword_acos_pct, rationale; pause_keyword: LP assigned 0 clicks AND keyword_acos_pct ≥ target — genuinely inefficient; expected_order_delta (orders/day lost by pausing, negative), expected_spend_delta (spend/day saved, negative); hold_keyword: LP assigned 0 clicks but keyword_acos_pct < target — keyword is EFFICIENT but outcompeted for budget by higher-CVR alternatives; do NOT pause it; monitor and revisit once budget is increased; expected_order_delta / expected_spend_delta show what is currently foregone due to budget constraint; increase_bid: estimated_order_uplift_per_10pct_bid, expected_spend_per_10pct_bid; decrease_bid: expected_order_delta (negative), expected_spend_delta (negative = savings); NOTE on decrease_bid when keyword_acos_pct < target_acos_pct: a decrease_bid on a keyword whose ACOS looks healthy does NOT mean the keyword is underperforming. LP maximises total orders under a budget constraint — it may allocate fewer clicks to a low-ACOS keyword when higher-CVR keywords (even at higher ACOS) produce more orders per dollar. The LP CVR estimate uses Beta-Binomial shrinkage, which can differ from raw observed ACOS. When writing the rationale for such an action, always state: 'LP reallocates clicks to higher-CVR keywords; this keyword ACOS ($val%) is healthy but other keywords generate more orders per dollar under the current budget constraint.' Do NOT imply the keyword is inefficient based on ACOS alone — cite the LP's click allocation logic.
    NOTE on pause_keyword and hold_keyword — THREE CRITICAL RULES:
    (1) Both actions mean LP assigns 0 clicks. Neither implies zero historical orders. FORBIDDEN: do NOT write 'zero orders despite spend', 'no conversions', or 'zero historical orders' unless keyword_acos_pct is null/missing. If keyword_acos_pct or estimated_cvr is present, the keyword HAS historical order data — state it as 'LP allocates 0 clicks in the optimal plan' instead.
    (2) hold_keyword = the keyword is EFFICIENT (ACOS below target) but excluded by budget constraint. NEVER describe it as underperforming, high-ACOS, or problematic. NEVER recommend pausing it. Correct framing: 'Efficient keyword (ACOS $val% < target $t%) held due to budget constraint — do not pause; increase budget to unlock this keyword.' The expected_order_delta / expected_spend_delta fields show current opportunity cost, not waste.
    (3) pause_keyword only appears when keyword_acos_pct ≥ target or keyword_acos_pct is null. Only in these cases is 'inefficient' or 'high ACOS' framing appropriate.
    NOTE on decrease_bid under inventory gate: when lp_summary.stock_gate_days > effective_stock_days AND there are lp_maxed_keywords, a decrease_bid action on a healthy-ACOS keyword is a PREPARATORY action — freed budget will automatically flow to maxed keywords once the inventory gate is cleared (inbound_shipped ETA is logistics-dependent and may clear sooner than estimated). Do NOT flag this as contradictory with the 'want more orders' goal; instead frame it as 'pre-positioning budget reallocation — takes effect when stock gate clears'; campaign_actions: expected_order_delta and expected_spend_delta quantify daily impact
    auto_mining_summary → aggregate stats for auto/PT search-term mining:
      auto_pt_cids_count: number of auto/PT campaigns analyzed
      terms_analyzed: total distinct search terms evaluated
      negative_count / harvest_count: candidates found
      total_wasted_spend / daily_wasted_spend: spend on zero-order terms (negative candidates)
      breakeven_spend: avg_price × target_acos — the max acceptable spend per order
      skipped: true if no auto/PT campaigns identified or no search term data
    auto_mining_beta_prior → Empirical Bayes Beta prior fitted from this ASIN's own search term CVR:
      alpha / beta: shape parameters; implied_cvr_pct = account-level CVR baseline
      n_terms_fitted: number of terms used to fit the prior (≥5 clicks each)
    auto_mining_negatives → negative keyword candidates (full list, up to 30):
      action: 'add_negative_keyword'
      priority: P0 (high confidence) or P1 (directional); gates: Gate A (EB expected_orders ≥ 3.0 → P0, ≥ 1.5 → P1, AND orders=0), Gate B (spend ≥ breakeven×1.5 → P0, ≥ breakeven×0.75 → P1, AND orders=0), Gate C (effective CPO > 3× breakeven → P1, even with orders > 0)
      suggested_match: EXACT (single-word) or PHRASE (multi-word)
      keyword_text, campaign_id, clicks, orders, spend_total, daily_spend, expected_orders_eb, rationale
    auto_mining_harvest → search terms to promote from auto to manual (full list, up to 20):
      action: 'harvest_to_manual'
      priority: P0 (≥5 orders, efficient ACOS) or P1 (2-4 orders, efficient ACOS)
      suggested_match: EXACT; suggested_bid: target_acos × avg_price × Wilson CVR lower bound
      keyword_text, campaign_id, clicks, orders, spend_total, acos_pct, rationale
    competitor_price_meta → credibility metadata for the competitor price dataset:
      n_competitors: number of distinct competitor ASINs sampled
      date_from / date_to: first and last date with price data (YYYY-MM-DD)
      avg_daily_count: average number of competitors with price data per day
    MANDATORY CITATION RULE — competitor price claims: whenever you cite a competitor price (min / max / median from competitor_price_summary), you MUST append sample metadata in parentheses: '(sampled from N competitors, date_from – date_to)'. If competitor_price_meta is null or n_competitors < 3, prefix the claim with '(limited sample — N competitors)' and treat it as directional only, NOT as a strong competitive benchmark. FORBIDDEN: do NOT write '$X vs $Y median' without immediately citing sample size and date range — a bare price comparison without sample metadata is an unsubstantiated claim.
    ad_traffic_ratio / organic_traffic_ratio / traffic_growth_7d → Xiyouzhaoci traffic score API (latest available snapshot). CRITICAL — traffic decline attribution rule: traffic_growth_7d reflects TOTAL traffic (ad + organic combined). When organic_traffic_ratio > 0.70, organic traffic dominates total traffic. In this case, FORBIDDEN: do NOT attribute a total traffic decline primarily to campaign pauses or budget cuts — ad traffic is a minority share and cannot explain a large total traffic drop. Instead, look for organic causes: (a) rating_delta < 0 → lower rating reduces organic click-through rate; (b) price premium vs competitors (price_mean vs competitor_price_summary median) → higher price suppresses organic conversion and search ranking; (c) rank_series showing declining organic rank (higher rank number) → organic position lost. Only when organic_traffic_ratio ≤ 0.30 can campaign pauses/budget cuts be cited as the primary driver of total traffic decline.
    rank_tracked_keywords / rank_series_days → Xiyouzhaoci daily organic rank trends; rank_series_days = days with rank data within data_start_date → data_end_date for the top tracked keyword
    market_trends_keywords → keywords with SFR weekly trend data (Xiyouzhaoci)
    change_attributions_count → change events that passed noise filter (Ads API history, data_start_date → data_end_date)
    causal_consensus_sample → consensus label of first change_attribution entry (ITS + CausalImpact + DML agreement for THAT SPECIFIC EVENT: Strong / Moderate / Weak / Confounded / Conflicting / Skipped). This is PER-EVENT model agreement — entirely separate from causal_reliability:
      causal_reliability = historical calibration quality (how accurate the model has been ACROSS PAST EVENTS)
      causal_consensus_sample = whether the 3 models agree ON THIS SPECIFIC EVENT
    These two can contradict: high causal_reliability + Conflicting consensus is valid and means 'the model is generally well-calibrated, but the 3 models disagree on this particular event — treat this event's causal direction as uncertain'. Do NOT use causal_reliability='high' to override a Conflicting/Weak consensus on a specific event.
    orders_reliability → statistical sufficiency of orders sample: high (≥100 orders), medium (30–99), low (<30)
    acos_ci_lo / acos_ci_hi → 95% ACOS confidence interval (Wilson method on CVR propagated to ACOS via ACOS = spend/sales; see Methodology section)
    backtest_hit_rate → percentage 0–100 (e.g. 85.0 = 85%); fraction of evaluated change events where model-predicted direction matched observed post-window KPI direction; threshold: <70 = near-random (causal_reliability='low'/'none'), ≥70 = reliable ('high')
    backtest_strong_hit_rate → same but restricted to 'Strong evidence' events only
    events_significant_count / events_significant_pct → how many change events in THIS run had at least one model produce a statistically significant result (p<0.05 + CI not crossing zero). events_significant_pct=0.0 means ALL events were insignificant in this run.
    causal_reliability → pre-computed AND-gate tier: 'high' requires BOTH backtest_hit_rate ≥70 AND events_significant_pct > 0; 'low' covers three sub-cases: (A) hit_rate not None and < 70 — calibrated but near-random; (B) hit_rate=None and events_significant_pct==0 — no calibration and no significance; (C) hit_rate=None and events_significant_pct>0 — significant results but no backtest history; 'none' = hit_rate=None or 0 AND events_significant_pct=0 or None — no data at all. Use this field to apply Rule 4 label-downgrade logic

  ```json
  {_summary_json}
  ```

  > **Evidence Summary**: 1-2 sentences citing the single most critical metric visible in the snapshot above (e.g., ACOS 45% vs. 30% warn threshold, budget exhausted 27/30 days, organic rank dropped from #12 → #28 over 10 days, can_sell_days=null → daily_sales not resolved). If a key field is null, call it out explicitly.

  ### Diagnostic Findings

  Each bullet must include a direct data citation in *italics*.

  - **Budget**: [Insight] — *Source: daily_budget=$val, budget_likely_exhausted=T/F over N days*
  - **Bids vs. Competition**: [Insight] — *Source: avg_bid=$val, high_acos_keywords=[kw1(ACOS%), kw2(ACOS%)]*
  - **Placement Strategy**: [Insight] — *Source: TOS ACOS=$val (modifier=$x%, spend_share=$y%) | PP ACOS=$val (modifier=$z%, spend_share=$w%) → mismatch if any*
  - **Keywords**: [Insight] — *Source: keyword_count=$n, match_type_dist={{exact:$a%, phrase:$b%}}, high_acos_keywords=[...]*
  - **Organic & Market**: [Insight] — *Source: ad_traffic_ratio=$val, organic_rank_trend=improving/stable/declining for kw=$kw; SFR trend up/down in week YYYY-Www → demand increased/decreased*
  - **Inventory**: [Insight] — *Source: inventory_risk=T/F, can_sell_days=$d*
  - **Profitability**: [Insight] — *Source: account_acos=$val%, TACOS=$val% (if tacos available), net_profit_after_ads=$val (if cogs+price provided)*
  - **LP Budget Optimisation**: [Insight — MUST distinguish two narratives] — If order_gap > 0: write 'Reallocating the current $X budget per LP plan could yield +Y orders/day (allocation fix — same total budget, different distribution); move spend away from above-target-ACOS lp_zero_keywords toward lp_maxed_keywords.' CRITICAL: only name a lp_zero_keyword here if its keyword_acos_pct > target_acos_applied. If a lp_zero_keyword has keyword_acos_pct ≤ target_acos_applied, it is budget-crowded-out — do NOT group it with above-target keywords; instead mention it under the budget-constraint narrative. If hold_keyword actions exist AND budget_binding=true: write separately 'Additionally, [keyword] (campaign [id]) is efficient (ACOS $val% < target) but budget-constrained — increasing total budget would unlock it.' NEVER merge these two into one sentence using 'budget constraint caused order_gap' — the order_gap is an allocation story, hold_keyword is a total-spend story. *Source: order_gap=$val, lp_zero_keywords=[kw1, kw2], lp_maxed_keywords=[kw3], lp_top_allocations=[kw4: $budget_share%], budget_binding=T/F*
  - **Change Attribution (past actions)**:
    - ✅ Positive: $change_type on $date → Δ orders = +$x (consensus: Strong/Moderate evidence, causal_reliability=high)
    - ❌ Negative: $change_type on $date → Δ orders = -$x (consensus: Strong/Moderate evidence, causal_reliability=high)
    - ⚠️ Uncertain: $change_type on $date → direction unclear (consensus=Conflicting OR causal_reliability=low/none — do NOT assert direction)
    - ⚠️ Confounded: $change_type coincided with promotion/price cut → effect cannot be isolated to bid/budget
    ⚠️ ICON RULE: ❌ Negative REQUIRES consensus startswith 'Strong evidence' OR 'Moderate evidence' AND dominant direction is negative AND causal_reliability ≠ 'none'. If consensus is 'Conflicting' OR causal_reliability is 'low' OR 'none', you MUST use ⚠️ Uncertain — NEVER ❌ Negative.

  ### Causal Confidence Assessment

  For each change_attribution entry cited above:
  - **Consensus**: Strong / Moderate / Weak / Confounded / Conflicting / Skipped (read from the `consensus` field directly — this is per-event model agreement, independent of causal_reliability; causal_reliability='high' means the model is historically well-calibrated, but a Conflicting consensus still means THIS event's direction is uncertain — do NOT use causal_reliability to override a Conflicting/Weak consensus)
  - **ITS** *(Linden 2015)*: if its.skipped=True write 'Skipped (reason: $reason)'; otherwise level_shift=$val [95% CI: level_shift_ci_lo – level_shift_ci_hi], p=$p_val (significant if p < 0.10)
  - **CausalImpact** *(Brodersen et al. 2015)*: if causal_impact.skipped=True write 'Skipped (reason: $reason)'; if shared_effect='duplicate' write '[same-day co-event — point_effect shared with primary; not additive]'; otherwise point_effect=$val [95% credible interval: ci_lo – ci_hi] (actual − BSTS counterfactual)
  - **DML** *(Chernozhukov et al. 2018)*: if dml.skipped=True write 'Skipped (reason: $reason)'; otherwise theta=$val [95% CI: theta_ci_lo – theta_ci_hi] (sandwich-SE; reliable only if r_squared ≥ 0)
  - **Historical calibration**: backtest_hit_rate=$val (stored as 0–1 fraction; display as percentage, e.g. 1.0 → '100%'; model direction vs observed post-window direction for this ASIN)
  - **Note**: Use 'causally demonstrated' only when consensus = 'Strong evidence' (all 3 models agree). Use 'suggested' or 'estimated' otherwise.
    When consensus = 'Conflicting' or causal_reliability ≠ 'high': use ⚠️ Uncertain (not ❌ Negative) regardless of delta_orders sign — the direction is not reliably established.
    When had_promotion=True or price_delta_window is large negative: do not attribute lift solely to the ad change — flag as partially confounded.
    Use competitor_price_summary {{min, max, median}} time series to explain sustained ACOS/CVR shifts that correlate with competitor price moves rather than internal bid changes. When citing any price from competitor_price_summary, always append '(N competitors, date_from – date_to)' from competitor_price_meta — do not cite competitor prices without sample size.
  - **CRITICAL — Same-day multi-event attribution**: Each change_attribution entry may carry two fields injected by the pipeline:
    • shared_effect = 'primary'   → this entry holds the representative BSTS measurement
    • shared_effect = 'duplicate' → this entry's causal_impact.point_effect AND delta_orders_normalized are the SAME ASIN-level values as the primary entry (single BSTS model / single YoY baseline); they are NOT independent estimates. delta_orders is also None when kpi_level='asin'. delta_baseline_source will be 'shared_with_primary'.
    • same_day_event_ids → list of event_ids that share the same changed_at date.
    Rules:
    1. Only cite point_effect and delta_orders_normalized from the 'primary' entry.
    2. For 'duplicate' entries these fields are already nulled by the pipeline; write '[same-day co-event — impact shared with primary; not additive]' instead of fabricating or repeating any numeric order impact.
    3. NEVER sum or imply point_effect × N for same-day events — the combined effect is the single primary measurement, not a multiple of it.
    4. delta_orders (campaign-level window) may still be present on duplicate entries when kpi_level='campaign'; it reflects that specific campaign's window shift, which is independent — you may cite it with that qualification.
    5. If shared_effect is absent, the event is unique on its date and the rule does not apply.

  ### Top 5 Prioritised Actions (most impactful first)

  For each action use this sub-structure:

  > **Action #N**: [Specific action with concrete numbers, e.g., increase TOS modifier from 10% → 30%]
  > - **Rationale**: [1-2 sentences with data citations]
  >   *Evidence: placement_performance.TOS.acos=$val, spend_share=$val%*
  > - **Expected impact**: [Quantified estimate] *(assuming CVR and CTR remain stable)*
  > - **Risks / caveats**: [What to monitor; set a time-box, e.g., 7 days. NEVER write 'None' — every action carries monitoring risk. For pause_keyword or decrease_bid: always note search-term coverage gap risk and recommend a review window. For hold_keyword: note that this is an opportunity-cost item, not a problem — recommend monitoring once budget increases. For increase_budget or increase_bid: always note ACOS drift risk and a time-box.]
  > - **Reference change attribution**: [Cite a past change_attribution entry if a similar action was taken; include the consensus confidence level. If none exists, write 'No historical reference available.']

  ### Account Health Summary

  - **Overall Health Score**: 🟢 / 🟡 / 🔴 (based on proportion of ASINs at each severity level)
  - **Biggest Win (Last 30d)**: $change_type on $date for ASIN $x → saved/earned $impact (cite change_attribution). CRITICAL rules when citing an event: (1) ALL attributes you cite — entity_type, keyword_text, campaign_id, date, DML/ITS/CI values — MUST come from the SAME single attribution entry. NEVER combine the DML of one event with the entity_type or date of a different event. (2) If no event has Strong or Moderate consensus, write 'No attributable win identified — all events show Conflicting consensus; causal direction cannot be determined.' Do NOT force-cite a Conflicting event as a win.
  - **Biggest Risk**: One sentence on the highest-severity unresolved issue across all ASINs
  - **Single Most Important Action (account-wide)**: Repeat the #1 action if it applies globally
  - **Monitoring Recommendation**: Which 1-2 metrics to track weekly

  ### Statistical Methodology

  | Method | Citation | Role in this report |
  | --- | --- | --- |
  | ITS | Linden (2015), *Stata J.* 15(2):480–500 | Piecewise OLS: y=α+β·t+γ·D+δ·(t−T₀)·D+ε; γ=level_shift (immediate step); 95% CI from OLS t-distribution SE |
  | CausalImpact | Brodersen et al. (2015), *Ann. Appl. Stat.* 9(1):247–274 | BSTS counterfactual; point_effect=actual−predicted; 95% posterior credible interval |
  | DML | Chernozhukov et al. (2018), *Econometrics J.* 21(1):C1–C68 | Frisch–Waugh–Lovell with RF residualisation; θ=clean causal effect; 95% CI from heteroscedasticity-robust sandwich SE |
  | LP | Dantzig (1963), *Linear Programming & Extensions* / OR-Tools | max Σorders_i·x_i s.t. Σx_i≤daily_budget, x_i≤headroom·clicks_i·bid_i |
  | ACOS CI | Wilson (1927), *JASA* 22:209–212 | 95% CI on CVR (Wilson score) propagated: ACOS_CI = ACOS_point × CVR_point / CVR_CI |

  *All CIs at 95% (α=0.05). Two significance thresholds are used for different purposes — this is intentional, not an inconsistency: (1) **Consensus voting** (ITS/DML direction votes in `_build_consensus`): p ≤ 0.10 one-sided — lenient so that weak signals still contribute a direction vote; the per-event ITS p-value shown in Causal Confidence Assessment uses this threshold. (2) **Backtest event significance** (`events_significant_pct`, `causal_reliability` gate): p < 0.05 AND 95% CI not crossing zero — strict; only events meeting both criteria count toward backtest_hit_rate calibration and the causal_reliability='high' AND-gate. When you see 'p < 0.10' in the ITS section it refers to threshold (1); when you see 'p < 0.05 + CI not crossing zero' in causal_reliability it refers to threshold (2).*

  ==== MANDATORY RULES (violations degrade report quality) ====

  1. Every claim must be accompanied by a direct data reference (field name + value from the JSON). No assertion without citation.
  2. When recommending a bid or budget change, provide a numerical bound (e.g., 'raise from $0.50 to $0.75') AND cite the pre-computed expected_order_delta / expected_spend_delta from keyword_actions or campaign_actions. Write it as: 'Expected: $X spend saved/day, −Y orders/day' or 'Expected: +$X spend/day, +Y orders/day (ACOS Z%)'. For increase_bid, cite estimated_order_uplift_per_10pct_bid. Cite a historical change_attribution entry if available.
  3. For any 'expected impact' estimate, state the assumption explicitly (e.g., 'assuming CVR and CTR remain stable; linear impression-share elasticity assumed'). Mark increase_bid order estimates as approximate: '~+N orders/day per +10% bid (impression elasticity unvalidated)'.
  4. Uncertainty labelling — two independent dimensions, BOTH must pass:
     Dimension A — causal_reliability (pre-computed AND-gate, ASIN-level):
     causal_reliability='high': backtest_hit_rate ≥70 AND ≥1 event statistically significant (p<0.05, CI not crossing zero) in this run — model is both historically calibrated AND currently significant.
     causal_reliability='low'/'none': at least one gate failed — downgrade all labels regardless of consensus.
     Dimension B — consensus (per-event model agreement):
     consensus='Strong evidence': all 3 models agree on direction for THIS event.
     consensus='Conflicting': models disagree on THIS event — direction is uncertain for this event EVEN IF causal_reliability='high'. Do NOT cite causal_reliability to override a Conflicting consensus.
     COMBINED RULE: use 'demonstrated'/'causally linked' ONLY when causal_reliability='high' AND consensus='Strong evidence' — both conditions required.
     When reporting both fields together, always explain the distinction: 'backtest_hit_rate=X% reflects historical model accuracy across N past events; the consensus for this specific event is Y — [interpret Y independently]'.
     causal_reliability='low' (any of: hit_rate 1–69%, or hit_rate=None with significant events, or hit_rate=None with no significant events): DO NOT use 'demonstrated'/'causally linked'; downgrade ALL consensus labels by one level (Strong→Moderate, Moderate→Weak, Weak→Inconclusive); prefix every causal citation with '(low causal reliability)'; do NOT promote any action to P0 solely on the basis of change_attribution evidence.
     causal_reliability='none' (backtest_hit_rate=0% or None AND events_significant_pct=0 or None): downgrade ALL consensus labels by one level AND treat all causal evidence as unvalidated; add '(backtest unvalidated)' after every citation; do NOT use 'demonstrated' or 'causally linked'.
     In ALL cases where causal_reliability ≠ 'high': LP-derived campaign_actions and keyword_actions retain their assigned priority (they are budget-optimisation signals independent of causal models), but do NOT cite change_attribution as primary justification for P0 actions.
  5. If confounders exist (promotion, price cut, competitor move), do not attribute the outcome solely to ad actions — rewrite the conclusion to reflect partial or unclear attribution.
  5a. Apparent contradictions between demand trend and traffic trend MUST be explained — do NOT leave them as unresolved coexisting statements. Specifically: if you write that seasonal demand is rising (e.g. SFR improving, market_trends showing week-over-week search volume increase) AND ALSO that ad clicks or traffic are declining, you MUST explain the root cause of the divergence. Possible explanations to check in order:
    (a) Promotion / price cut: had_promotion=True or a large negative price_delta_window in covariate_series → higher CVR may sustain orders despite lower clicks; write: 'Rising demand coincides with active promotion/price cut — higher CVR compensates for lower click volume; verify whether orders hold when promotion ends.'
    (b) Bid reduction: if a decrease_bid or decrease_budget change_attribution event precedes the click decline with positive consensus, write: 'Click decline follows [change_type] on [date] — intentional bid cut reduced impression share; seasonal demand rising but not captured due to lower bids.'
    (c) Budget exhaustion driving impression cap: if budget_likely_exhausted=True, write: 'Budget cap is reached — rising demand is constrained by ad budget, not bid level; impression share limited by spend ceiling.'
    (d) Ad share shift: if organic rank is improving (rank_series showing lower rank number), write: 'Organic rank improving — ad traffic may be declining as organic captures a larger share of total demand.'
    (e) If none of the above apply: write explicitly 'Root cause of traffic decline vs rising demand is unclear from available data — check search term report for impression share loss and competitor activity.'
  FORBIDDEN: do NOT write both 'seasonal demand is rising' and 'clicks are declining' in the same report without a connecting explanation for the divergence.
  6. Append a **Caveats & Data Quality** subsection at the end of each ASIN report if ANY of the following are true:
     - lp_summary.spend_ceiling_bound == true: write 'LP click-ceiling bound: LP optimal spend $lp_optimal_spend vs LP-scope campaign budget $lp_scope_campaign_daily_budget (LP-scope historical spend $lp_scope_daily_spend/day). Click ceilings on LP-scope keywords are exhausted — LP cannot allocate the full budget. Non-LP campaigns (auto/PT) run on their own separate budgets ($non_lp_scope_daily_spend/day). Priority action: expand keyword coverage or raise bids on ceiling-constrained keywords.' CRITICAL RULES when spend_ceiling_bound=true:
       (a) Do NOT cite a negative order_gap as evidence the current strategy is optimal.
       (b) Do NOT recommend increasing the budget — more budget does NOT help ceiling-bound keywords.
       (c) Do NOT recommend decrease_bid for any keyword — cutting bids lowers future click ceilings and freed budget cannot reach ceiling-constrained keywords. keyword_actions with action='review_bids' under ceiling-bound means MONITOR, not cut.
       (d) Do NOT suggest that lp_optimal_spend ($X) vs historical_daily_spend ($Y) means campaigns are under-spending — the gap is explained by non-LP scope spend (lp_summary.non_lp_scope_daily_spend).
     - lp_summary.order_gap < 0 AND lp_summary.budget_binding == false AND lp_summary.spend_ceiling_bound == false: write 'LP order estimate below actual (order_gap=$Z, no binding constraint identified): possible CVR data gap or keyword mix mismatch — review bid strategy before scaling budget.'
     - lp_summary.budget_binding == true AND lp_summary.order_gap < 0: this is expected and MUST NOT be flagged as a caveat. Reason: pessimistic CVR shrinkage (Beta-Binomial Wilson shrinkage) systematically under-estimates orders vs actual when the global budget is fully consumed — this is a known model artifact, NOT a real performance gap. STRICTLY FORBIDDEN: do NOT write any caveat, note, or qualification about LP order estimate being below actual when budget_binding == true AND order_gap < 0. campaign_actions already encodes the correct recommendation for this scenario.
     - lp_summary.placement_data_unknown == true: do NOT recommend TOS/PP placement modifiers — write 'Placement data unavailable (all traffic reported as UNKNOWN)' and omit any placement-specific action items.
     - campaign_match_strategy == 'name_substring' or 'none': write 'Campaign matching used name-substring fallback — some campaigns may be misattributed. Verify campaign_ids and treat campaign-level metrics with caution.' Do NOT make high-confidence campaign-level recommendations under name_substring matching.
     - causal_reliability != 'high': write the appropriate caveat below (causal_reliability is pre-computed — use it directly; do NOT re-derive from raw fractions):
       The three 'low' sub-conditions are MUTUALLY EXCLUSIVE — evaluate in order A → B → C and apply exactly ONE:
       Sub-condition A — backtest_hit_rate is not None AND backtest_hit_rate < 70 (check first): 'Causal model directional accuracy $pct% (threshold 70%) — change_attribution evidence is near-random; all consensus labels downgraded one tier. ($pct = backtest_hit_rate × 100, e.g. 0.65 → 65%.) LP budget recommendations remain valid (independent of causal models).' Apply ONLY when backtest_hit_rate is not None AND backtest_hit_rate < 70. Do NOT apply alongside Sub-condition B or C.
       Sub-condition B — backtest_hit_rate is None AND events_significant_pct == 0 (no calibration data AND no significant events in this run): 'No change event in this run produced a statistically significant causal estimate (p≥0.05 or CI crosses zero across all models). Causal labels are descriptive only — do not use them to justify P0 actions.' Apply ONLY when backtest_hit_rate IS None AND events_significant_pct == 0. If events_significant_count ≥ 1, do NOT write this caveat.
       Sub-condition C — backtest_hit_rate is None AND events_significant_pct > 0 (no calibration history, but this run produced statistically significant events): 'Causal model has no historical calibration data (backtest_hit_rate unavailable) — directional accuracy across past events is unverified. This run produced statistically significant estimates ($pct% of events, p<0.05), but reliability cannot be confirmed without backtest history. Treat causal labels as preliminary — use consensus and CI width, not causal_reliability, to judge strength.' ($pct = events_significant_pct × 100.) Apply ONLY when backtest_hit_rate IS None AND events_significant_pct > 0.
       causal_reliability='none': 'Causal analysis has no calibration data and no significant results — all change_attribution evidence is unvalidated; treat as exploratory.' Apply ONLY when causal_reliability == 'none' (backtest_hit_rate is None or 0 AND events_significant_pct == 0 or None).
       This caveat is MANDATORY when causal_reliability is 'low' or 'none'.
     - Any change_attribution entry has attribution_suspect == true: write 'Attribution outlier: [event_date] [change_type] delta_orders=$X exceeds 1.5× pre-window mean ($pre_mean/day) on ASIN-level KPI fallback — seasonal or account-wide trend may dominate; treat delta magnitude as an upper bound only.' Do NOT use the raw delta_orders value as a factual order loss — qualify it as an estimate.
     - placement_performance is missing or empty
     - keyword_count < 10
     - More than half of change_attribution entries have skipped=True for all three causal models
     - post_window.days < 5 for the majority of attributions
  7. Paused campaigns: when campaign_state='PAUSED', do NOT recommend bid or budget changes for those campaigns — such changes have no effect until the campaign is re-enabled. CRITICAL: if a campaign_action entry has campaign_state='PAUSED' AND action='decrease_budget' or 'pause_candidate', treat this as a data artefact — DO NOT surface it as an action item. The campaign is already paused; any historical ACOS figure is moot until the campaign is re-enabled. For paused campaigns, only surface an action if it explicitly says 'enable_and_review_bids', 'enable_and_increase_budget', or 'archive_candidate'. If all campaigns are paused (active_campaign_count == 0), the #1 priority action must address the pause decision before any other optimisation. CRITICAL — connect change_attribution to inv_gate: if change_attributions show that pausing campaigns caused a drop in orders (delta_orders < 0, change_type='STATUS', direction='ENABLED→PAUSED') AND campaign_actions contains 'enable_and_*' actions that carry a prerequisite (inventory gate), explicitly state in the Change Attribution section: 'Re-enabling these campaigns is currently blocked by the inventory prerequisite (effective_stock_days={{X}} < stock_gate_days={{Y}}). Prioritise FBA replenishment to unlock these campaigns.' Do NOT omit this linkage — it explains why the Top 5 actions do not include a direct re-enable recommendation.
  8. Statistical sufficiency: when orders_reliability = 'low' (<30 orders total), mark ACOS and CVR estimates as 'statistically preliminary — results may shift significantly with more data' and display the ACOS 95% CI (acos_ci_lo%–acos_ci_hi%) alongside the point estimate in the Snapshot table. When orders_reliability = 'medium' (30–99 orders), add a note that conclusions should be validated over a longer window before committing to large bid or budget changes (risk of regression-to-the-mean).
  9. **Auto/PT search-term mining** (auto_mining_negatives / auto_mining_harvest):
     - If auto_mining_summary.skipped == true: omit this section entirely.
     - If auto_mining_negatives is non-empty: add a sub-section 'Auto Campaign Negative Keywords'. List P0 candidates first, then P1. For each entry show: keyword_text, campaign_id, suggested_match, spend_total ($), orders, and rationale. P0 = high confidence (strong statistical or spend signal); P1 = directional (add to watch list). State the daily_wasted_spend total upfront: 'Est. $X/day wasted on zero-order terms (Gate A/B).'
     - If auto_mining_harvest is non-empty: add a sub-section 'Search Terms Ready for Manual Harvest'. List P0 first, then P1. For each entry show: keyword_text, campaign_id, suggested_match=EXACT, suggested_bid ($), orders, acos_pct (%), and rationale.
     - Do NOT surface auto_mining_beta_prior diagnostics in the report — it is internal model metadata.
     - auto_mining candidates are independent of LP keyword_actions — do NOT compare bids or budgets across the two sections.
  10. **LP-vs-causal action conflicts** (action_conflicts field, only non-empty when causal_reliability='high'):
     - If action_conflicts is empty: ignore this rule entirely.
     - If action_conflicts is non-empty: for each conflict entry, locate the matching campaign_action or keyword_action in the Top 5 list. Demote its priority by one tier (P0→P1, P1→P2, P2→omit from Top 5). Under that action's 'Risks / caveats' add: '⚠ Historical conflict ({{conflict_change_type}} on {{conflict_event_date}}): a similar {{action}} event previously correlated with an order decline ({{conflict_delta_orders}}/day, {{conflict_consensus}}). Validate this action against the historical pattern before executing.' Do NOT remove the action from the report — demotion + caveat is the correct response. Do NOT apply this rule when causal_reliability is 'low' or 'none' — conflict detection is gated on reliable causal models.
  11. **Chart placeholders**: Diagnostic charts will be injected after the report is generated. Place each marker at the most relevant position inside the matching section. Do NOT write URLs or image syntax — just the marker text. Omit a marker if that data is unavailable (e.g. no change events → omit `[CHART:its_causal]`).
     Available markers:
     - `[CHART:daily_trend]` — daily orders/ACOS time series; place in Quick Metrics Snapshot or Overview
     - `[CHART:its_causal]` — ITS/CausalImpact causal chart; place in Causal Confidence Assessment
     - `[CHART:kw_quadrant]` — keyword ACOS × orders quadrant; place in Diagnostic Findings / Keywords
     - `[CHART:placement_bar]` — placement ACOS vs bid adjustment; place in Diagnostic Findings / Placement
     - `[CHART:inventory_burndown]` — inventory burn-down; place in Diagnostic Findings / Inventory
     - `[CHART:comp_price_box]` — competitor price distribution; place in Diagnostic Findings / Organic & Market
     - `[CHART:lp_waterfall]` — LP budget waterfall; place in Diagnostic Findings / LP Budget Optimisation
     - `[CHART:rank_trend]` — organic rank trend; place in Diagnostic Findings / Organic & Market

  Begin the report now.
